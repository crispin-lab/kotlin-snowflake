name: "🔄CI & CD"
on:
  push:
    branches:
      - main

jobs:
  ci:
    name: "🔨"
    uses: ./.github/workflows/ci.yml
    secrets: inherit

  cd:
    needs:
      - ci
    name: "🛠️"
    uses: ./.github/workflows/cd.yml
    secrets: inherit
